# semantic_code_finder
This repository is a semantic code finder which uses chonkie to traverse the ASt code and ingest into qdrant. Qdrant senatic search will get the top K relevant code snippets and show them on to the UI.
